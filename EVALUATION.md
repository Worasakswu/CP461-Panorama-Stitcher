# ผลการประเมิน — 2026-09-24

เอกสารนี้เปรียบเทียบโค้ดเวอร์ชันแรกบน branch `main` (commit `3555092`) กับ pipeline ใหม่ ทั้งสองเวอร์ชันถูกรันด้วยสคริปต์เดียวกัน [`scripts/evaluate.py`](scripts/evaluate.py) โดย baseline ในสคริปต์คัดลอกโค้ดเวอร์ชันแรกมาทั้งหมด ได้แก่ SIFT → BFMatcher + ratio 0.75 → `findHomography` (RANSAC 5 px) → warp ภาพที่สองลง canvas ขนาด (w₁+w₂, max(h₁, h₂)) แล้ววางภาพแรกทับ

> **ข้อควรทราบ:** ตัวเลขทั้งหมดในเอกสารนี้มาจาก **ภาพสังเคราะห์ที่รู้ homography จริง** จึงวัดความแม่นยำได้ละเอียด แต่ยังไม่ใช่ภาพถ่ายจริง ฉากในภาพสังเคราะห์เป็นระนาบ homography จึงอธิบายความสัมพันธ์ได้พอดี และยังไม่มี parallax หรือวัตถุเคลื่อนที่ (ดูหัวข้อ [ข้อจำกัด](#ข้อจำกัด))

## สรุปผล

- **ต่อภาพสำเร็จ 70/70 ชุด** ใน 7 สถานการณ์ ส่วนเวอร์ชันแรกสำเร็จ **10/40** และทำได้เฉพาะกรณี 2 ภาพ
- ถ้าอัปโหลดสลับลำดับ (ขวา→ซ้าย) เวอร์ชันแรกสำเร็จ **0/10** เพราะภาพซ้ายถูกฉายไปพิกัดติดลบแล้วหลุดกรอบ (coverage เหลือ 62%) ส่วนเวอร์ชันใหม่สำเร็จ 10/10
- ความคลาดเคลื่อนของตำแหน่งภาพ (corner error) เฉลี่ยไม่เกิน **0.7 px** ทุกสถานการณ์ รวมถึงชุด 5 ภาพที่สลับลำดับ
- หาลำดับซ้าย→ขวาเองถูกต้อง **20/20** ชุด และตัดภาพที่ไม่เกี่ยวข้องออกได้ **10/10** ชุด
- เมื่อแสงแต่ละภาพต่างกัน ขั้นความสว่างที่รอยต่อ (seam step P99) ลดลงประมาณ **41%** (2.71 → 1.60) จาก exposure compensation + multi-band blending
- ชุดกล้องหมุนมุมกว้างรวม ~180° ต่อครบ **5/5** ชุด (สลับเป็น cylindrical อัตโนมัติ) โดยประมาณ focal length คลาดเคลื่อนเฉลี่ย **1.0%**
- เวลาเฉลี่ยกรณี 2 ภาพ 640×480: เวอร์ชันแรก 0.06 s, เวอร์ชันใหม่ 0.23 s (เพิ่ม verification, exposure compensation และ multi-band blending)

## ผลต่อสถานการณ์

สุ่ม 10 ชุดภาพต่อสถานการณ์ ภาพขนาด 640×480 เวอร์ชันแรกรันได้เฉพาะสถานการณ์ที่มี 2 ภาพ

| สถานการณ์ | ภาพ | สำเร็จ (เดิม) | สำเร็จ (ใหม่) | Corner error px (เดิม → ใหม่) | Coverage (เดิม → ใหม่) | PSNR dB (เดิม → ใหม่) | Seam step P99 (เดิม → ใหม่) |
|---|---:|---:|---:|---:|---:|---:|---:|
| A. 2 ภาพ ซ้าย→ขวา ซ้อนกัน 40% | 2 | 3/10 | 10/10 | 0.23 → 0.24 | 96.3% → 99.4% | 28.95 → 28.81 | 0.12 → 0.35 |
| B. 2 ภาพ อัปโหลดสลับ (ขวา→ซ้าย) | 2 | 0/10 | 10/10 | 0.22 → 0.24 | 62.0% → 99.3% | 29.22 → 28.55 | 0.09 → 0.49 |
| C. 2 ภาพ แสงต่างกัน ±20% + vignetting | 2 | 3/10 | 10/10 | 0.36 → 0.35 | 95.0% → 99.3% | 20.32 → 21.05 | 2.86 → 1.79 |
| D. 2 ภาพ ซ้อนกันเพียง 25% | 2 | 4/10 | 10/10 | 0.56 → 0.40 | 95.3% → 99.3% | 28.21 → 28.45 | 0.17 → 0.43 |
| E. 3 ภาพ มุมเอียงมาก + แสงต่างกัน | 3 | รองรับแค่ 2 ภาพ | 10/10 | — → 0.41 | — → 99.2% | — → 24.11 | — → 1.19 |
| F. 5 ภาพ สลับลำดับ + แสงต่างกัน | 5 | รองรับแค่ 2 ภาพ | 10/10 | — → 0.68 | — → 99.1% | — → 22.43 | — → 1.90 |
| G. 4 ภาพ + ภาพที่ไม่เกี่ยวข้อง 1 ภาพ | 5 | รองรับแค่ 2 ภาพ | 10/10 | — → 0.59 | — → 99.3% | — → 24.12 | — → 1.53 |

วิธีอ่านตาราง:

- **Homography ของเวอร์ชันแรกแม่นพอ ๆ กับเวอร์ชันใหม่** (corner error ใกล้กัน) แต่เวอร์ชันแรกล้มเหลวตอนนำภาพมาวางรวมกัน canvas มีขนาดตายตัวและวางภาพแรกไว้ที่มุมซ้ายบนเสมอ ส่วนของภาพที่ถูกฉายไปทางซ้าย บน หรือล่าง จึงหายไป ทำให้ coverage ของสถานการณ์ A, C, D เหลือราว 95–96% และ B เหลือ 62%
- ในสถานการณ์ A, B, D ที่แสงเท่ากันทุกภาพ ค่า seam step ของทั้งสองเวอร์ชันต่ำกว่า 0.5 ซึ่งตามองไม่เห็นรอยต่อ ค่าของเวอร์ชันใหม่สูงกว่าเล็กน้อยเพราะ seam ถูกวางกลางส่วนซ้อนทับ ทำให้ใช้พิกเซลจากภาพที่ผ่าน resampling มากกว่าภาพอ้างอิง ส่วนนี้ไม่ใช่รอยต่อที่ตาเห็น
- ในสถานการณ์ที่แสงต่างกัน (C) PSNR เพิ่มขึ้นและ seam step ลดลงชัดเจน

## Ablation ของวิธีรวมภาพ

ใช้ภาพที่ warp แล้วชุดเดียวกัน (สถานการณ์ A–G, รวม 70 ชุด) แล้วเปลี่ยนเฉพาะวิธีรวมภาพ

| วิธีรวมภาพ | PSNR (dB) ↑ | Seam step P99 ↓ แสงเท่ากัน (A, B, D) | Seam step P99 ↓ แสงต่างกัน (C, E, F, G) |
|---|---:|---:|---:|
| วางทับ (แบบเวอร์ชันแรก) | 24.87 | 0.332 | 2.705 |
| Feather | 25.09 | 0.373 | 1.765 |
| Multi-band | 25.06 | 0.423 | 1.797 |
| **Multi-band + Exposure compensation (ค่าเริ่มต้น)** | **25.36** | 0.422 | **1.601** |

- ตอนแสงต่างกัน การ blend แบบใดก็ตามลดขั้นความสว่างที่รอยต่อได้ประมาณ 35% เมื่อเทียบกับการวางทับ และ exposure compensation ช่วยลดเพิ่มอีก (รวม −41%)
- ตอนแสงเท่ากัน ทุกวิธีได้ค่าต่ำกว่า 0.5 จึงมองไม่เห็นรอยต่อ ความต่างเล็กน้อยระหว่างวิธีมาจาก resampling ตามที่อธิบายไว้ในหัวข้อก่อนหน้า
- Feather กับ multi-band ได้ตัวเลขใกล้กันบนภาพสังเคราะห์ เพราะภาพ align กันระดับ sub-pixel จึงไม่เกิดเงาซ้อน ข้อดีของ multi-band จะเห็นชัดในภาพจริงที่ align ไม่สมบูรณ์ เพราะรายละเอียดความถี่สูงถูกผสมในช่วงแคบ ขณะที่ feather เฉลี่ยทั้งส่วนซ้อนทับจนเกิดภาพซ้อน/เบลอ (ดูแท็บ “เปรียบเทียบ Blending” บนเว็บ)

## กล้องหมุนมุมกว้าง

กล้องหมุนรอบจุดเดิม 5 ภาพ หมุนครั้งละ 30° มุมรับภาพแนวนอน 60° (ครอบคลุมรวมประมาณ 180°) และ pitch สั่นเล็กน้อย ±1.5°

| ชุดภาพ | ต่อครบ 5 ภาพ (Auto → Cylindrical) | บังคับ Planar แล้วถูกปฏิเสธพร้อมแจ้ง error | Focal error เฉลี่ย | เวลาเฉลี่ย |
|---:|---:|---:|---:|---:|
| 5 | 5/5 | 5/5 | 1.0% | 0.86 s |

ระนาบเดียวรองรับมุมรวมระดับนี้ไม่ได้ เพราะภาพริมจะถูกฉายเลยเส้นขอบฟ้า ระบบจึงตรวจพบและเปลี่ยนเป็น cylindrical projection เอง แทนที่จะสร้างภาพที่บิดเบี้ยว

## SIFT เทียบ ORB

สถานการณ์ E และ F (รวม 20 ชุด) เกณฑ์สำเร็จคือต่อครบทุกภาพ และ corner error เฉลี่ยน้อยกว่า 2 px

| Detector | สำเร็จ | Corner error (px) | เวลาเฉลี่ย (s) |
|---|---:|---:|---:|
| SIFT (ค่าเริ่มต้น) | 20/20 | 0.54 | 0.72 |
| ORB | 4/20 | 6.37 | 0.63 |

ORB ต่อภาพได้และเร็วกว่าเล็กน้อย แต่ตำแหน่ง keypoint บน pyramid ชั้นหยาบไม่มี sub-pixel refinement แบบ SIFT ภาพจึงเหลื่อมกันหลาย px ระบบจึงใช้ SIFT เป็นค่าเริ่มต้น และเก็บ ORB ไว้เป็นตัวเลือกสำหรับเปรียบเทียบ

## ตัวชี้วัด

- **สำเร็จ:** ต่อครบทุกภาพที่ควรต่อ (ภาพแปลกปลอมต้องถูกตัดออก), corner error เฉลี่ย < 2 px และ coverage ≥ 98%
- **Corner error:** ระยะเฉลี่ย (px) ระหว่างมุมทั้ง 4 ของแต่ละภาพเมื่อฉายด้วย transform ที่ระบบหาได้ กับเมื่อฉายด้วย homography จริง วัดในพิกัดของภาพอ้างอิง
- **Coverage:** สัดส่วนพื้นที่ที่กล้องถ่ายไว้ทั้งหมด ซึ่งปรากฏในภาพผลลัพธ์ นับรวมส่วนที่หลุดออกนอก canvas ด้วย
- **PSNR:** เทียบภาพผลลัพธ์กับฉากจริงที่ render ลงพิกัดเดียวกันแบบไม่มี noise/แสงเพี้ยน (ตัดขอบนอก 5 px)
- **Seam step P99:** เปอร์เซ็นไทล์ที่ 99 ของขนาด gradient ของ residual (ผลลัพธ์ − ฉากจริง) หลัง Gaussian blur σ = 3 px ในแถบรอบส่วนซ้อนทับ blur ช่วยกรอง noise และความต่างจาก interpolation ทิ้ง ให้เหลือเฉพาะ “ขั้น” ความสว่าง รอยต่อแข็งจึงได้ค่าสูง ส่วนการไล่ระดับกว้าง ๆ ได้ค่าต่ำ หน่วยเป็นระดับสีต่อพิกเซล
- **Focal error:** |f ที่ประมาณได้ − f จริง| / f จริง

## Protocol และการทำซ้ำ

- **ภาพสังเคราะห์** ([`src/synthetic.py`](src/synthetic.py)): ฉากประกอบด้วยสีไล่ระดับ ลวดลายความถี่กลาง รูปทรงและตัวอักษรสุ่มกว่า 300 ชิ้น และ grain ละเอียด แต่ละภาพคือ footprint บนฉากที่เลื่อนตาม overlap โดยสุ่มมุมทั้ง 4 ด้วย N(0, 3–6% ของความกว้าง) เพื่อจำลอง perspective และเลื่อนแนวตั้ง ±5% จากนั้นจำลองกล้อง: gain ต่อภาพ U(1 ± 0–20%) พร้อมสีเพี้ยน ±3%, vignetting 1 − v·r² (v = 0–0.3), Gaussian noise σ = 1.5 และ JPEG quality 92
- **ชุดกล้องหมุน:** render จากฉาก 360° บนทรงกระบอกรัศมีเท่ากับ focal จริง (554 px)
- **Seeds:** ชุดที่ k ของสถานการณ์ X ใช้ seed = 100·ord(X) + k (k = 0–9) ทุกตัวเลขรันซ้ำได้ด้วย `python scripts/evaluate.py` ผลจะอยู่ใน `evaluation_outputs/` (CSV รายชุด + ภาพตัวอย่าง)
- **การตั้งค่า:** เวอร์ชันใหม่ใช้ค่าเริ่มต้นทั้งหมด ยกเว้นปิด auto-crop เพื่อวัด coverage/PSNR บน canvas เต็ม (auto-crop ตรวจแยกใน unit test ว่าไม่เหลือขอบดำ)
- **สภาพแวดล้อม:** Windows 11, Intel Core i5-12400F, RAM 16 GB, Python 3.10.11, OpenCV 5.0.0, NumPy 2.2.6, Streamlit 1.64.0 เวลาเป็นค่าจากการรันครั้งเดียวบนเครื่องนี้ และจะช้ากว่านี้บน Streamlit Community Cloud
- **ชุดทดสอบอัตโนมัติ:** `python -m pytest -q` → **24 passed** ครอบคลุม SIFT/ORB, ratio test, ภาพไม่มี feature, RANSAC กับ outliers 30%, การปฏิเสธคู่สุ่ม/เมทริกซ์ที่กลับด้าน/ข้ามเส้นขอบฟ้า, การประมาณ focal, ภาพสลับลำดับ, ภาพแปลกปลอม, ภาพไม่ซ้อนทับ, cylindrical, gain compensation, blending, seam, auto-crop และหน้าเว็บจริงผ่าน Streamlit AppTest (ภาพตัวอย่าง, อัปโหลดไฟล์, error)

## สิ่งที่เปลี่ยนจากเวอร์ชันแรก

| ส่วน | เวอร์ชันแรก | เวอร์ชันใหม่ |
|---|---|---|
| จำนวนภาพ | 2 ภาพ ต้องเรียงซ้าย→ขวา | 2–10 ภาพ ลำดับใดก็ได้ |
| Matching | ratio test, crash เมื่อไม่มี descriptor | ratio test + ตัดคู่ที่ชี้จุดซ้ำ, รองรับภาพไม่มี feature, เลือก SIFT/ORB ได้ |
| Geometry | RANSAC ครั้งเดียว ไม่ตรวจผล | RANSAC ทุกคู่ + verification (Brown & Lowe, ความสมเหตุสมผลของเมทริกซ์) + image graph + reference กลางโครงข่าย |
| Projection | Planar เท่านั้น | Planar หรือ Cylindrical อัตโนมัติ + ประมาณ focal จาก homography |
| Canvas | ขนาดตายตัว (w₁+w₂) ภาพหลุดกรอบได้ | คำนวณจากมุมทุกภาพ ย่อผลลัพธ์เมื่อใหญ่เกินหน่วยความจำ |
| Blending | วางทับ เห็นรอยต่อ | Gain compensation + distance-transform seam + multi-band (เลือก feather/วางทับเพื่อเทียบได้) |
| ขอบดำ | ไม่ตัด | Auto-crop สี่เหลี่ยมที่ใหญ่ที่สุดที่ไม่มีขอบดำ |
| หน้าเว็บ | ภาพ “Inliers” ที่จริงแสดงคู่จุดทั้งหมด | ตารางทุกคู่ภาพ, inlier/outlier แยกสี, keypoints, แผนผังการวางภาพ, เปรียบเทียบ blending, เวลาแต่ละขั้น, ดาวน์โหลด |
| Input | `cv2.imdecode` ไม่หมุนตาม EXIF, ไม่ย่อภาพ | หมุนตาม EXIF, ย่อภาพตั้งแต่ตอน decode, แจ้งไฟล์ที่อ่านไม่ได้ |

## สิ่งที่ทดลองแล้วไม่ได้นำมาใช้

- **Global refinement ของ homography 8 DOF ทุกภาพพร้อมกัน** (Levenberg–Marquardt บน inliers ทุกคู่): ทดลองกับชุด 6 ภาพ 3 ชุดต่อกรณี ในกรณีภาพซ้อนกัน 40% (ไม่มีคู่ภาพที่ปิดวง) corner error เฉลี่ยแย่ลงจาก 0.71 เป็น 0.79 px ส่วนกรณีซ้อนกัน 60% (มีคู่ปิดวง 4 คู่) แทบไม่เปลี่ยน (0.26 → 0.27 px) และ RMS ของจุดคู่กันลดลงน้อยกว่า 0.5% เหตุผลคือ homography 8 DOF ต่อภาพมีพารามิเตอร์มากเกินกว่าข้อมูลจะบังคับได้ จึงไม่ได้นำมาใช้ ทางที่ถูกกว่าคือ bundle adjustment ของการหมุนกล้อง + focal (ราว 4 DOF ต่อภาพ) แบบ Brown & Lowe
- **ตัววัด gradient error ในส่วนซ้อนทับ:** ตัววัดนี้ลงโทษความเบลอจาก resampling ของภาพที่ไม่ใช่ภาพอ้างอิงแทนที่จะวัดรอยต่อ จึงเปลี่ยนมาใช้ seam step P99 ของ residual หลัง low-pass
- ระหว่างประเมินผลพบและแก้บั๊กของ multi-band ที่ทำให้ขอบนอกของพาโนรามามืด (error ในระยะ 5 px จากขอบลดจาก 13.5 เหลือ 5.9 ระดับสี ใกล้เคียงแบบวางทับที่ 5.8) และบั๊ก distance transform ที่ขอบ ROI ซึ่งทำให้ seam เอียงไปด้านหนึ่ง

## ข้อจำกัด

- ยังไม่มีผลเชิงปริมาณจากภาพถ่ายจริง ภาพสังเคราะห์เป็นฉากระนาบที่ไม่มี parallax, วัตถุเคลื่อนที่, lens distortion หรือ rolling shutter ตัวเลขจึงเป็นขอบบนของความแม่นยำที่คาดได้ในภาพจริง
- Noise model (gain, vignetting, Gaussian noise, JPEG) ยังง่ายกว่ากล้องจริง เช่นไม่มี tone curve ที่ไม่เป็นเชิงเส้น หรือ white balance ที่เปลี่ยนมาก
- Error สะสมตามจำนวนภาพ (0.24 px ที่ 2 ภาพ → 0.68 px ที่ 5 ภาพ) เพราะยังไม่มี bundle adjustment
- Cylindrical ทดสอบเฉพาะการหมุนแนวนอน ยังไม่รองรับ 360° ที่ต่อปิดวง หรือพาโนรามาหลายแถว
- เวลาวัดบนเครื่อง desktop ภาพ 640×480 ภาพจากมือถือถูกย่อเหลือด้านยาว 1,200 px จึงใช้เวลามากกว่านี้

## ประเมินด้วยภาพถ่ายจริงของกลุ่ม

ใส่ภาพไว้ใน `test_images/<ชื่อชุด>/` (โฟลเดอร์ละ 1 พาโนรามา) แล้วรัน

```bash
python scripts/evaluate.py --real test_images
```

สคริปต์จะรายงานจำนวนภาพที่ต่อได้, projection, inliers/inlier ratio/RMSE ของคู่ภาพที่ใช้, เวลา และคำเตือน แล้วบันทึกพาโนรามาของเวอร์ชันใหม่และของเวอร์ชันแรก (2 ภาพแรก) ไว้ใน `evaluation_outputs/real/` เพื่อเทียบด้วยตา ภาพจริงไม่มี ground truth จึงควรรายงานผลส่วนนี้แยกจากตารางข้างบน
